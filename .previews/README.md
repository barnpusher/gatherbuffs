# Previews

Place release screenshots for WowUp Hub in this folder.

Notes:

- Use `.png` or `.jpg` files.
- Keep the files in the branch you tag for release.
- WowUp snapshots the contents of `.previews/` at tag time, so the images can vary by release.
